class Program
{
    static int sumar(int numeroEspecial)
    {
        return numeroEspecial;
    }
    static void Main(string[] args)
    {
        /*
         * 55 - Escribe una función 
         * recursiva que calcule el factorial de un número.

         */
        int numero = sumar(numeroEspecial : 7);
        Console.WriteLine(numero);

    }
}